<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Todo – <%= todo.id %></title>
  </head>
  <body>
    <h1><%= todo.title %> – <%= todo.completed ? 'Done' : 'Not completed' %></h1>
    <p><%= todo.description %></p>
  </body>
</html>
